---
import BaseLayout from '~/layouts/BaseLayout.astro'
import { getCollection, render } from 'astro:content'

const entry = (await getCollection('adopt')).find(e => e.id === 'index')
if (!entry) return Astro.redirect('/404')
const { Content } = await render(entry)
const title = entry.data.title || 'Adopt PubID'
---

<BaseLayout title={title} description={entry.data.description || ''}>
  <article class="mx-auto max-w-3xl px-6 py-16 prose prose-pubid">
    <Content />
  </article>
</BaseLayout>
